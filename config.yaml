defaults:
  - meta: config.yaml
  - default_styling: config.yaml
  - _self_

version: 0.0.9

scene: 2D

slides:

  - title: We'll look at .. 
    number: "   "
    content:
      - type: items
        bullets:
          - "Introduction"
          - "Traditional Organizational Structures"
          - "Technological Disruptions"
          - "Shifts in Management"
          - "Evolution of Communication"
          - "Cultural Shifts in Organizations"
          - "Future Trends"
          - "Challenges in Adapting to Technological Changes"
          - "Conclusion"
        distance: 3
        anchor: "{{ title }}"

  - title: Introduction
    number: "1.0"
    content:
      - type: text
        text: "Organizations have undergone continuous change:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to:
          target: "{{ title }}"
          dir: LEFT
        no_next_slide: false
      - type: text 
        text: "Driven by evolving technologies, globalization, and workforce dynamics."
        next_to:
          target: "{{ last }}"
          dir:  DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: "Objective: To explore the impact of technological disruptions on three key areas: "
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        weights:
          - text: "Objective"
            weight: BOLD
        no_next_slide: false    
      - type: items
        bullets:
          - Management Structures
          - Communication Processes
          - Organizational Culture
        distance: 1
        font_size: "self.vs_size"
        anchor: "{{ last }}"
      - type: text
        text: "Why This Matters:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        weights:
          - text: "Why This Matters:"
            weight: BOLD
        no_next_slide: false
      - type: text
        text: "   ‣ Technology dictates how businesses operate, compete, and grow"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: "   ‣ Understanding these changes is crucial for adapting to the future of work"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
  - title: Traditional Organizational Structures
    number: "2.0"
    content:
      - type: text
        text: "Management:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to:
          target: "{{ title }}"
          dir: LEFT
        weights:
          - text: "Management:"
            weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Hierarchical and centralized."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Clear chain of command with authority concentrated at the top."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
          
      - type: text
        text: "Communication:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to:
          target: "{{ title }}"
          dir: LEFT
        weights:
          - text: "Communication"
            weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Linear and top-down"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Primarily face-to-face, with formal memos and written reports."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT

      - type: text
        text: " Culture:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to:
          target: "{{ title }}"
          dir: LEFT
        weights:
          - text: "Culture"
            weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Stability and predictability were prioritized over innovation."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Employee roles were rigid, with little room for autonomy or creativity."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT

  - title: Technological Disruptions
    number: "3.0"
    content:
      - type: text
        text: "Technological advancements have disrupted traditional structures, leading to:"
        next_to:
          target: "{{ title }}"
          dir: 3*DOWN
        align_to: 
          target: "{{ title }}"
          dir: LEFT

      - type: text
        text: "01 : The Digital Revolution:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "The Digital Revolution"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Introduction of personal computers, the internet, and cloud computing"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
            

      - type: text
        text: "02 : Globalization:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
          - text: "Globalization"
            weight: BOLD
        no_next_slide: false    
      - type: text
        text: " ‣ Technology enabled businesses to operate globally."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT

      - type: text
        text: " 03 : Automation & Artificial Intelligence (AI):"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
          - text: "Automation & Artificial Intelligence (AI)"  
            weight: BOLD
        no_next_slide: false    
      - type: text
        text: " ‣ Routine tasks automated, shifting focus to knowledge-based work and innovation."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image
        image: Screenshot 2024-12-03 at 21-18-40 CEO.png
        scale: 0.8
        shift: 3.4*DOWN
  - title: Shifts in Management
    number: "4.0"
    content: 
      - type: text
        text: " 4.1 From Hierarchical to Flat Structures:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "4.1 From Hierarchical to Flat Structures"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Decentralized decision-making."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Empowered teams with greater autonomy and accountability."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " 4.2 Agile and Lean Management:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "4.2 Agile and Lean Management"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Focus on iterative processes, rapid feedback, and continuous improvement."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Tech companies adopting Agile methodologies."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " 4.3 Data-Driven Decision Making:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "4.3 Data-Driven Decision Making"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Leveraging big data and analytics for strategic decisions."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      
  - title: From Hierarchical to Flat Structures
    number: "4.1"
    content: 
      - type: text
        text: " Traditional Hierarchical Structure :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Traditional Hierarchical Structure"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " A structure with multiple layers of authority and a top-down management approach"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Clear authority lines."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Centralized decision-making."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Vertical Communication."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Benefits :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Benefits"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Clear accountability."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Effective for large organizations."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " Drawbacks :"
        next_to:
          target: "{{ title }}"
          dir: 11.5*DOWN
        align_to: 
          target: "{{ title }}"
          dir: RIGHT
        weights:
           - text: "Drawbacks"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Slow decision-making."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false     
      - type: text
        text: " ‣ Limited flexibility."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image
        image: Screenshot 2024-12-03 at 19-30-36 CEO.png
        scale: 0.6
        shift: RIGHT*5 + DOWN
      - type: reset  
        no_next_slide: false
      - type: text
        text: " Flat Organizational Structure :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Flat Organizational Structure"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " A structure with fewer management layers, promoting collaboration and autonomy"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Fewer layers of management."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Decentralized decision-making."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Open communication."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Benefits :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Benefits"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Faster decision-making."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Increased collaboration."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " Drawbacks :"
        next_to:
          target: "{{ title }}"
          dir: 11.5*DOWN
        align_to: 
          target: "{{ title }}"
          dir: RIGHT
        weights:
           - text: "Drawbacks"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Role confusion."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false     
      - type: text
        text: " ‣ Requires self-management."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: image
        image: Screenshot 2024-12-07 at 10-30-30 What Is a Flat Organizational Chart Explained with Examples.png
        scale: 1.1
        shift: RIGHT*3 + DOWN*2
  - title: Agile & Lean Management
    number: "4.2"
    content: 
      - type: text
        text: " Agile Management :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Agile Management"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " An iterative approach to project management that emphasizes flexibility and collaboration "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: image
        image: Screenshot 2024-12-07 at 11-11-54 What Is Agile Project Management Types Methods & Benefits.png
        scale: 1.1
        shift: DOWN
      - type: reset       
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Iterative and incremental processes with short cycles."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Strong focus on collaboration through cross-functional teams."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Customer-centric approach that incorporates feedback during each cycle."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Benefits :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Benefits"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Adaptability to changes, ensuring the product aligns with customer needs."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Enhanced collaboration improves team morale and problem-solving."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Drawbacks :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        weights:
           - text: "Drawbacks"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ May lack predictability due to frequent changes in scope."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false     
      - type: text
        text: " ‣ Requires high commitment and active involvement from all stakeholders."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: reset    
      - type: text
        text: " Lean Management :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Lean Management"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: "A systematic methodology focused on creating value for the costumer "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image
        image: Screenshot 2024-12-07 at 11-33-41 The ultimate guide to Lean project management.png
        scale: 1.1
        shift: DOWN + RIGHT*3.2
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-08 at 06-29-22 Le guide complet du Lean Management.png
        scale: 1.2
        shift: DOWN + LEFT*3 
      - type: reset  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Emphasizes minimizing waste (e.g., overproduction, waiting times, defects...)."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Continuous improvement and focus on value-added activities."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Employee empowerment, encouraging team involvement in problem-solving."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Benefits :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Benefits"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Cost efficiency and streamlined workflows."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Improved quality and consistency in processes and outputs."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Drawbacks :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        weights:
           - text: "Drawbacks"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Implementation challenges due to resistance to change."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false     
      - type: text
        text: " ‣ Risk of focusing too much on efficiency, potentially neglecting innovation."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
  - title: Data-Driven Decision Making
    number: "4.3"
    content:
      - type: text
        text: Making organizational decisions based on verified and analyzed data
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to:
          target: "{{ title }}"
          dir: LEFT
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-08 at 20-31-35 What Is Data-Driven Decision Making for Business Success.png
        scale: 1.1
        shift: DOWN*0.8 + LEFT*3
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-08 at 20-31-24 What Is Data-Driven Decision Making for Business Success.png
        scale: 1.1
        shift: DOWN*0.8 + RIGHT*4
  - title: Evolution of Communication
    number: "5.0"
    content: 
      - type: text 
        text: " 5.1 Traditional Communication:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "5.1 Traditional Communication"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ DFace-to-face meetings, memos, and emails"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Delayed feedback and limited collaboration"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " 5.2 Modern Communication:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "5.2 Modern Communication"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Instant Messaging & Collaboration Tools: Slack, Microsoft Teams"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Remote Communication: Video conferencing tools like Zoom and Google Meet"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Asynchronous Work: Employees can collaborate across time zones."
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false

  - title: Traditional Communication
    number: "5.1"
    content:
      - type: text 
        text: " Formal, hierarchical, and offline organizational communication methods "
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Information flows top-down or through fixed channels"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Heavy reliance on memos, letters, and structured meeting"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Delayed feedback due to slower communication methods"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Frequent reliance on physical presence for communication"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " Limitations :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Limitations"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Delayed information flow across departments"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Limited Reach: Constrained by geography and access to physical tools"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Inability to adapt to real-time changes or needs"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-08 at 21-28-50 The Pyramid Of Communication - How To Improve Your Team Communication.png
        scale: 0.9
        shift: RIGHT*5 
      
  - title: Cultural Shifts in Organizations
    number: "5.2"
    content:
      - type: text 
        text: " Flexible, collaborative, and tech-driven organizational communication methods "
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Instant communication via messaging apps and video calls"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Encourages teamwork through shared platforms like Slack, Teams, or Trello"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Information flows freely across levels and departments"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Frequent reliance on physical presence for communication"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Advantages :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Advantages"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Instantaneous communication across the globe"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Easy to reach team members regardless of location"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Combines communication with data analytics and tracking"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-08 at 23-25-48 Increase Your Bandwidth With These 3 Tech-Savvy Speaking Tips.png
        scale: 0.9
        shift: RIGHT*5 + DOWN*1.6
  - title: Cultural Shifts in Organizations
    number: "6.0"
    content:
      - type: text 
        text: " 6.1 Traditional Culture:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "6.1 Traditional Culture"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Emphasis on conformity, stability, and clear roles"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Innovation was often limited to specific departments"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: "6.2 Modern Culture:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "6.2 Modern Culture"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Innovation-Driven: Encouraging experimentation and risk-taking"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Employee-Centric: Focus on well-being, diversity, and inclusion"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Collaborative: Cross-functional teams and open communication"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
  - title: Traditional Culture
    number: "6.1"
    content:
      - type: text 
        text: " A hierarchical, rule-driven, and stability-focused organizational culture "
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Emphasis on policies, procedures, and formalized processes"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Prioritizes predictability and minimizing risks"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Performance is evaluated at the individual level"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Challenges :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Challenges"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Difficulty adapting to new trends and market demands"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Inhibits creativity and innovation"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Can lead to dissatisfaction due to rigid hierarchies"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-09 at 09-36-57 types-of-organizational-culture.webp (WEBP Image 1024 × 680 pixels).png
        scale: 0.8
        shift: RIGHT*4 + DOWN
  - title: Modern Culture
    number: "6.2"
    content:
      - type: text 
        text: " A dynamic, collaborative, and innovation-focused organizational culture "
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣  Encourages collaboration and reduces hierarchical barriers "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Embraces change and thrives on continuous improvement"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Emphasizes collective goals and performance"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: "  Advantages :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Advantages"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Fosters creativity and new ideas"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Empowers individuals, improving job satisfaction"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Breaks silos for better teamwork and integration"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-09 at 12-53-07 Organizational Culture Definition Importance and Development.png
        scale: 1.1
        shift: RIGHT*4 + DOWN
  - title: Future Trends in Organizational Structures
    number: "7.0"
    content:
      - type: text 
        text: " 7.1 Remote-First Organizations:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "7.1 Remote-First Organizations"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Distributed teams as the new normal"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Challenges: Managing remote teams, maintaining culture"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " 7.2 AI-Augmented Organizations:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "7.2 AI-Augmented Organizations"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ AI and machine learning assisting in decision-making and routine tasks"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Employees focusing on strategic and creative roles"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text 
        text: " 7.3 Networked Organizations:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "7.3 Networked Organizations"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Companies operating as ecosystems, partnering with other firms and freelancers"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
#          
  - title: Remote-First Organizations
    number: "7.1"
    content:
      - type: text
        text: Designed to prioritize remote work, with flexible policies for global teams
        next_to: 
          target: "{{ title }}"
          dir: DOWN*4
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Decentralized work environments "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Heavy reliance on collaboration tools "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Focus on outcomes rather than office hours"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Asynchronous workflows for global teams"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image
        image: Screenshot 2024-12-09 at 13-53-54 Remote-First Company Everything You Need to Know.png
        scale: 1.1 
        shift: RIGHT*3 + DOWN
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-09 at 15-00-44 Work From Anywhere Jobs The Companies Allowing Work From Anywhere - Remote Work.png
        scale: 0.9 
        shift:  LEFT * 4 + 2*DOWN
      - type: reset    
      - type: text
        text: " Advantages :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Advantages"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Access to a global talent pool "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Increased work-life balance for employees "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Cost savings on physical office spaces"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Challenges :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Challenges"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣  Maintaining engagement and collaboration "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣  Cybersecurity risks in decentralized systems "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣  Isolation and reduced team cohesion"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣  Difficulty in monitoring performance"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
  - title: AI-Augmented Organizations 
    number: "7.2"
    content:
      - type: text
        text: Structures that integrate AI to enhance decision-making, operations, and innovation
        next_to: 
          target: "{{ title }}"
          dir: DOWN*4
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ AI-driven analytics for real-time insights "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Automation of repetitive tasks to boost efficiency "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ AI-powered communication tools for seamless interactions"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Adaptive AI systems for continuous learning"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image
        image: Screenshot 2024-12-09 at 19-19-48 Unleashing the Power of Generative AI Transforming ADDIE.png
        scale: 0.9 
        shift: UP*0.5 + RIGHT*4.3
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-09 at 19-20-54 Cogent Blog Adapting To The AI Revolution Understanding The Integration Of Artificial Intelligence In The Workforce.png
        scale: 0.9 
        shift:  RIGHT*4.3 +  1.9*DOWN
      - type: reset    
      - type: text
        text: " Advantages :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Advantages"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Access to a global talent pool "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Increased work-life balance for employees "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Cost savings on physical office spaces"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " Challenges :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Challenges"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣  Maintaining engagement and collaboration "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣  Cybersecurity risks in decentralized systems "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣  Isolation and reduced team cohesion"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣  Difficulty in monitoring performance"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: image
        image: Screenshot 2024-12-09 at 19-21-03 Cogent Blog Adapting To The AI Revolution Understanding The Integration Of Artificial Intelligence In The Workforce.png 
        scale: 1
        shift: RIGHT*3.5 + UP*0.8
  - title: Networked Organizations
    number: "7.3"
    content:  
      - type: text
        text: Flexible, interconnected teams replacing rigid hierarchies
        next_to: 
          target: "{{ title }}"
          dir: DOWN*4
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        no_next_slide: false
      - type: text   
        text: " Characteristics :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Characteristics"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Cross-functional collaboration "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Decentralized decision-making "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Dynamic team structures for agility"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Fluid, project-based roles"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: image  
        image: Screenshot 2024-12-09 at 22-05-32 1 new message.png
        scale: 1.1
        shift: DOWN*0.3 + RIGHT*4
      - type: reset  
      - type: text
        text: " Advantages :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Advantages"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Faster response to changes and challenges"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Improved innovation through diverse perspectives"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Breaks down silos for better teamwork"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text
        text: " Challenges :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Challenges"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Balancing autonomy with organizational alignment"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Coordination complexity in large networks"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Resource allocation issues due to fluid roles"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: image
        image: Screenshot 2024-12-10 at 00-01-26 Four Network Security Challenges for Organizations with a Remote Workforce by Colleen Martin Medium.png
        scale: 0.8
        shift: RIGHT*4 + DOWN*0.5
  - title: Challenges in Adapting to Technological Changes
    number: "8.0"
    content:
      - type: text 
        text: " 8.1 Resistance to Change:"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "8.1 Resistance to Change:"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Employees and managers may resist adopting new technologies"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text
        text: " 8.2 Cybersecurity :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Cybersecurity"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Increased reliance on digital tools raises security concerns"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text 
        text: " 8.3 Skill Gaps:"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "8.3 Skill Gaps"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ Need for continuous upskilling to adapt to new technologies"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: image
        image: Screenshot 2024-12-10 at 13-11-32 RE.png
        scale: 0.8
        shift: RIGHT*4.5
  - title:  Resistance to Change
    number: "8.1"
    content:
      - type: text 
        text: " Leads to slower innovation and lagging behind competitors "
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "8.1 Resistance to Change:"
             weight: BOLD
        no_next_slide: false
      - type: image
        image: Screenshot 2024-12-10 at 13-27-36 Resistance to Change Organizational Behavior and Human Relations.png
        scale: 1.2
        shift: UP*0.8 + RIGHT*4.5
      - type: text   
        text: " Reasons ? :"
        next_to:
          target: "{{ title }}"
          dir: 8*DOWN
        align_to: 
          target: "{{ title }}"
          dir: LEFT
        weights:
           - text: "Reasons"
             weight: BOLD
        no_next_slide: false     
      - type: text
        text: " ‣ The lack of understanding of the benefits creates uncertainty "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ The mindset of “if it isn’t broken, why fix it?”  "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Concerns about job replacement by automation"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false  
      - type: text
        text: " ‣ Poorly communicated reasons and benefits for the change lead to misunderstandings"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: image
        image: Screenshot 2024-12-10 at 14-21-20 RE.png
        scale: 1.2
        shift: DOWN*2
  - title:  Cybersecurity
    number: "8.2"
    content:
      - type: text 
        text: " Risks :"
        next_to:
          target: "{{ title }}"
          dir: 4*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Risks"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Increased Attack Surface; new digital tools and systems"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Data Breaches; leads to financial loss and legal penalties"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false
      - type: text
        text: " ‣ Insufficient Security Measures; outdated security systems"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Third-Party Risks; give attackers full control of the organization’s systems and data"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
      - type: text 
        text: " Solutions ? :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:
           - text: "Solutions"
             weight: BOLD
        no_next_slide: false
      - type: text
        text: " ‣ Employee Training; on recognizing suspicious emails "
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Data Backup and Recovery Plans"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Regularly evaluate systems for vulnerabilities and threats"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: image
        image: Screenshot 2024-12-10 at 15-42-08 What is cybersecurity McKinsey.png
        scale: 0.8
        shift: DOWN*1.3 + RIGHT*4.6
        no_next_slide: false 
      - type: image
        image: Screenshot 2024-12-10 at 14-34-04 Overview of Organizational Cybersecurity - Download Whitepaper (PDF) » OnlineWhitePapers.com.png
        scale: 0.7
        shift: UP*0.9 + RIGHT*4.6

  - title:  Skill gaps
    number: "8.3"
    content:
      - type: text 
        text: " Reasons :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:  
           - text: "Rreasons"
             weight: BOLD
          

      - type: text
        text: " ‣ Rapid Technological Advancements"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Insufficient Training Programs"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Resistance to Learning (8.1)"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        
      - type: text 
        text: " Impacts :"
        next_to:
          target: "{{ last }}"
          dir: 2*DOWN
        align_to: 
          target: "{{ last }}"
          dir: LEFT
        weights:  
           - text: "Impacts"
             weight: BOLD
      - type: text
        text: " ‣ Increased Reliance on External Expertise"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Higher Turnover Rates"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: text
        text: " ‣ Lower Employee Morale"
        next_to:
          target: "{{ last }}"
          dir: DOWN
        align_to:
          target: "{{ last }}"
          dir: LEFT
        no_next_slide: false 
      - type: image
        image: Screenshot 2024-12-10 at 16-34-46 How to Identify & Close Knowledge and Skills Gap at Work.png
        scale: 0.7
        shift: RIGHT*4 + UP*1.3
        no_next_slide: false 
      - type: image
        image: Screenshot 2024-12-10 at 16-35-22 How to Identify a Skills Gap in the Workplace Tips and Best Practices.png
        scale: 0.7
        shift: RIGHT*4 + DOWN*1.8

